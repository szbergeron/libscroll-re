(library
  (name libscroll-re)
  (public_name libscroll-re)
  (foreign_stubs
    (language c)
    (names )
    (flags -I./include))
  (c_library_flags (-L./rust/target/debug/liblibscroll_re.so)))

(ignored_subdirs (node_modules))
